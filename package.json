{
  "name": "smart-wms-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "seed": "node backend/datasets/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
